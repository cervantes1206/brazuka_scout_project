# Brazuka Scout Fútbol — Mockup

Mockup de diseño (landing page) para la cotización del proyecto Brazuka Scout Fútbol.

## Contenido
- `index.html` — mockup estático de una sola página (HTML/CSS/JS, sin dependencias de build).

## Estado
Este es un **mockup visual**, no el sitio funcional. No incluye backend (correo automatizado, base de datos de aplicaciones, moderación real).

## Cómo verlo localmente
Abrir `index.html` directamente en el navegador.
